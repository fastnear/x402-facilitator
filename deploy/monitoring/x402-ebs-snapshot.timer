[Unit]
Description=x402 host EBS snapshot daily

[Timer]
# After the database backup (03:43) so the snapshot includes the freshest
# on-host dump as well.
OnCalendar=*-*-* 04:15:00 UTC
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
