/target/
/.env
/.env.*
!/.env.example
*.pem
*.key
*.credential
*.secret
coverage/
dist/
.DS_Store

# Instance runtime configs carry per-deploy values; only the .example templates
# are tracked. (Applies to NEAR and eip155 configs alike.)
/deploy/config/*.json
!/deploy/config/*.json.example
